import java.util.List;

public class Observador implements Elemento {
    @Override
    public void mover(String item, List<String> from, List<String> to) {
        // No se implementa ya que el observador se mueve solo
    }
}